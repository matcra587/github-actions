# security.yml's zizmor job intentionally inherits caller permissions so SARIF
# upload can be enabled without requiring security-events for annotation-only
# callers.
rules:
  excessive-permissions:
    ignore:
      - security.yml
